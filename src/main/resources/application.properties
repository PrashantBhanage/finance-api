spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/fintrack_db}
spring.datasource.username=${DB_USERNAME:finuser}
spring.datasource.password=${DB_PASSWORD:finpass123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

server.port=${PORT:8080}

jwt.secret=${JWT_SECRET:zorvyn-finance-super-secret-key-for-hmac-sha256-algorithm-2026}
jwt.expiration=86400000